
$Y**LIST OF CONSOLE COMMANDS**

$C(GENERAL COMMANDS)

  $M*$W OUT

  $M*$W EDIT

  $M*$W WHEN

  $M*$W RUN

  $M*$W TIMES

  $M*$W LIBRARY

  $M*$W TASK

  $M*$W DELETE

  $M*$W DELETE_ALL

$C(LISTING COMMANDS)

  $M*$W LIST_VARIABLES

  $M*$W LIST_LIBRARIES

  $M*$W LIST_CONSOLE_COMMANDS 

  $M*$W LIST_CUSTOM_COMMANDS

$C(HELP & DOCUMENTATION)

  $M*$W HELP

  $M*$W DOC_GENERAL

  $M*$W DOC_COMMANDS

  $M*$W DOC_CONSOLE_COMMANDS

  $M*$W DOC_CUSTOM_COMMANDS

  $M*$W DOC_VARIABLES

  $M*$W DOC_DATATYPES

  $M*$W DOC_OPERATIONS

  $M*$W DOC_OUTPUT